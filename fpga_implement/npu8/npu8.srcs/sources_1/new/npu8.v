module npu8 # (parameter QBIT = 8, FIXED_INT = 16, FIXED_FRAC = 16, ADR_WIDTH = 10)
    (
    input clk,
    input reset_x,

    //port A
    ouput [ADR_WIDTH-1:0] RADR_A,
    input [QBIT:0] RDATA_A.



    );
endmodule
